


//noinspection JSAnnotator
const accessToken;

const Spotify = {

  getAccessToken(){
    if(accessToken) {
      return accessToken;
    } else {

      window.location.href.match(accessToken, expires_in)

    }
  }
};







export default Spotify;